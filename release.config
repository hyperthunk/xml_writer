
{extend, "rebar.config"}.
{deps, [
    {rebar_dist_plugin, "0.0.6",
        {git, "git://github.com/hyperthunk/rebar_dist_plugin.git"}}
]}.

{plugins, [rebar_dist_plugin]}.

{dist, [
    {format, zip},
    {assembly, project}
]}.
